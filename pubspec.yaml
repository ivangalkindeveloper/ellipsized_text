name: ellipsized_text
description: One line ellipsized text with the ability to adjust the position of the ellipsis.
version: 1.0.0
homepage:

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
